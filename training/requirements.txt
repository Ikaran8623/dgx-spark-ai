# Training / Fine-tuning Requirements for DGX Spark
# Tested with Python 3.12, CUDA 13.0, NVIDIA GB10
#
# Install: pip install -r training/requirements.txt

# Unsloth for efficient QLoRA fine-tuning (2x faster)
unsloth[colab-new] @ git+https://github.com/unslothai/unsloth.git
unsloth_zoo @ git+https://github.com/unslothai/unsloth-zoo.git

# Core ML
torch>=2.3.0
transformers>=4.43.0
datasets>=2.20.0
trl>=0.9.0
peft>=0.12.0
accelerate>=0.33.0
bitsandbytes>=0.43.0

# Data
jsonlines>=4.0.0

# Export
llama-cpp-python>=0.2.80

# Serving fine-tuned models
fastapi>=0.104.0
uvicorn>=0.24.0
